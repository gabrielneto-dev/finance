# Constraints

- The repository currently exposes almost no domain detail beyond the name `finance`.
- Confirmed knowledge must come from repository files or explicit user input.
- Unknown project scope must remain labeled as assumption or question, not fact.
- The context system should stay compact until the project gains more real artifacts.
